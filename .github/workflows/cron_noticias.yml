name: Actualizar Noticias Jurídicas
on:
  schedule:
    - cron: '0 4 * * *' # 05:00 AM hora española
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip' # Esto ayuda a que no olvide las librerías entre ejecuciones
          
      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 lxml feedparser
          
      - name: Ejecutar Script
        env:
          LINKEDIN_TOKEN: ${{ secrets.LINKEDIN_TOKEN }}
        # Añadimos -m para asegurar que use las librerías del entorno actual
        run: python actualizar_noticias.py
        
      - name: Subir cambios
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name 'GitHub Action'
          git config --global user.email 'action@github.com'
          git add noticias.json historico_noticias.csv
          git commit -m "v53: Corrección de dependencias y ejecución robusta" || echo "Sin cambios"
          git push origin main
